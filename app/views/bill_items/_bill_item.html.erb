  <tr>
    <td><%= bill_item.therapy.select_name %></td>
    <td class="qty"><%= bill_item.quantity %></td>
    <td class="price"><%= bill_item.unit_price %></td>
    <td class="price"><%= bill_item.total %></td>
    <td><%= link_to 'Edit', edit_bill_item_path(bill_item) %></td>
    <td><%= link_to 'Destroy', bill_item, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
