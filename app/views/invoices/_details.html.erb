<% if !@invoice.auths.empty? %>
  <%= render 'auths/list_auths', :auths => @invoice.auths %>
<% end -%>

<% if !@invoice.payments.empty? %>
  <%= render 'payments/list_payments', :payments => @invoice.payments %>
<% end -%> 

<%= link_to 'Add New Auth', new_invoice_auth_path(@invoice) %> |
<%= link_to 'Add New Payment', new_payment_path %> |
<%= link_to 'List Invoices', invoices_path %>
