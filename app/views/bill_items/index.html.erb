<h1>Listing bill_items</h1>

<table>
  <tr>
    <th>Auth</th>
    <th>Therapy</th>
    <th>Quantity</th>
    <th>Unit price</th>
    <th>Total</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bill_items.each do |bill_item| %>
  <tr>
    <td><%= bill_item.auth %></td>
    <td><%= bill_item.therapy %></td>
    <td><%= bill_item.quantity %></td>
    <td><%= bill_item.unit_price %></td>
    <td><%= bill_item.total %></td>
    <td><%= link_to 'Show', bill_item %></td>
    <td><%= link_to 'Edit', edit_bill_item_path(bill_item) %></td>
    <td><%= link_to 'Destroy', bill_item, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bill item', new_bill_item_path %>
