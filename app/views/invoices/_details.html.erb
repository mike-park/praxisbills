<% if !@invoice.auths.empty? %>
  <%= render 'auths/auths', :auths => @invoice.auths %>
<% end -%>

<% if !@invoice.payments.empty? %>
  <%= render 'payments/payments', :payments => @invoice.payments %>
<% end -%> 

<%= link_to 'Add New Patient Authorization', new_invoice_auth_path(@invoice) %> |
<%= link_to 'Add New Payment', new_payment_path(invoice_id: @invoice.id) %> |
<%= link_to 'List Invoices', invoices_path %>
